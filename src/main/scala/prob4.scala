object Problem4 extends App {
	def isPalindrome(a : Int, b : Int) : Boolean {

	}

	def maxPalindrome
	var maxPalindrome = 0

	for (i <- 100 to 999) {
		for (j <- 100 to 999) {
			println (i * j).toString
		}
	}
}
